name: recsys-course

docker_env:
    image: recsys
    volumes: [
        "$(pwd)/data:/home/data/"
    ]

entry_points:

    main:
        parameters:
            data: string
            recsys: string
        command: "python -m recsys_course.train -d {data} -r {recsys}"
